name: test-reports
on:
  workflow_run:
    workflows:
      - run-unit-tests             
    types: 
      - completed

permissions:
  contents: read
  actions: read
  checks: write

jobs:
  report: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
